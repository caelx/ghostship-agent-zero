# OpenCode Go Provider

Adds `opencode_go` as an Agent Zero chat provider.

Set `OPENCODE_GO_API_KEY` before starting Agent Zero. Model discovery uses `https://opencode.ai/zen/go/v1/models` and Agent Zero parses OpenAI-compatible `data[].id` entries through its normal model search path.
